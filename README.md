LLM TSF Leaderboard

Repositório contendo o benchmark e scripts para avaliação de modelos de forecasting de séries temporais utilizado no projeto.

Estrutura principal

- `data/` : datasets utilizados nas experiências.
- `leaderboard/` : código da interface e utilitários do benchmark.

Datasets

Veja o dataset card completo em `data/DATASET_CARD.md`.

Se quiser, posso adicionar instruções de execução (ex.: criar ambiente e rodar experimentos).
